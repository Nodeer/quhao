# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Press
*		/									module:press

# Ignore favicon request
GET     /favicon.ico                        staticFile:public/images/applyful_favicon.gif

GET     /                     		  		LandingController.index

# Routes for Android & IOS clients
# Routes for Android & IOS clients

# Category
GET		/allCategories						MerchantController.allCategories

# Merchant
GET		/nextPage							MerchantController.nextPage
GET		/merchantByCategory					MerchantController.merchantByCategory
GET		/merchant							MerchantController.merchant
GET		/quhao								MerchantController.quhao
GET		/getTopMerchants					MerchantController.getTopMerchants
GET		/nahao								MerchantController.nahao

# Accounts
GET		/accounts							rsecure.Secure.accounts
GET		/signup								AccountController.signup
GET		/login								AccountController.login

# Patch
*		/p/{action}							Patches.{action}
GET		/p        							Patches.index

# backend management
*		/b/m/{action}						backend.MerchantManagementController.{action}
*		/b/w									backend.{action}
# backend management

# Map static resources from the /app/public folder to the /public path
GET     /public/                            staticDir:public

#Image
*		/imgs								ImageController.loadImage

# Catch all
*       /{controller}/{action}              {controller}.{action}
*       /back/{controller}/{action}         backend.{controller}.{action}